import Constants from "expo-constants";

const { manifest } = Constants;

// export const accountUrl = `http://${manifest.debuggerHost.split(':').shift()}:44388/`;
export const accountUrl = "http://reactnativemobile.gear.host/";